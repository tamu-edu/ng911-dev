exclude_dirs:
  - test_suite/checks
  - test_suite/tests
  - test_suite/services/config/config_service_old.py

skips:
  - B101
  - B314 # as we intentionally work with PIDF-LO, HELD, LIS, Location XML and other XML for tests and need to see everything for analysis
  - B405 # the same
  - B607 # we often use indirect passed, controllable
  - B404 # we use subprocess intentionally
  - B112 # on purpose
  - B603 # we use it often and intetionally
  - B311 # we often use random