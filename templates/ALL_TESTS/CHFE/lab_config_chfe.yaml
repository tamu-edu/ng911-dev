lab_config:
  pca_certificate_file: ""  # TLS certificate file for secure connections
  pca_certificate_key: ""   # TLS private key matching the certificate
  test_suite_host_ip: "10.140.140.253"    # IP of the machine running the test suite
  entities:
    # Test System OSP
    - name: "OSP"
      mode: "STUB_SERVER"
      function: "OSP"
      role: ["SENDER"]
      fqdn: ""
      certificate_file: ""
      certificate_key: ""
      api:
        tel_number: ""
      interfaces:
        - name: "IF_OSP_BCF"
          fqdn: ""
          ip: "10.140.140.130"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""  #  Leave empty if not required
          dns: [ ]  #  Leave empty if not required
          port_mapping:
            - name: "IF_OSP_BCF_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_OSP_CHFE"
          fqdn: ""
          ip: "10.140.140.131"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""  #  Leave empty if not required
          dns: [ ]  #  Leave empty if not required
          port_mapping:
            - name: "IF_OSP_CHFE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_OSP_BRIDGE"
          fqdn: ""
          ip: "10.140.140.132"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""  #  Leave empty if not required
          dns: [ ]  #  Leave empty if not required
          port_mapping:
            - name: "IF_OSP_BRIDGE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_OSP_TS-CHFE"
          fqdn: ""
          ip: "10.140.140.133"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""  #  Leave empty if not required
          dns: [ ]  #  Leave empty if not required
          port_mapping:
            - name: "IF_OSP_TS-CHFE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
    # Test System BCF
    - name: "BCF"             # Simulated BCF stimulus generator
      mode: "STUB_SERVER"     # Acts as a stub server for SIP signaling
      function: "BCF"         # Border Control Function
      role: ["OTHER", "SENDER"]          # Initiates SIP calls in test scenarios
      fqdn: ""                # Fully qualified domain name for routing
      certificate_file: ""    # X.509 certificate for TLS
      certificate_key: ""     # Private key for TLS
      interfaces:
        - name: "IF_BCF_OSP"
          fqdn: ""
          ip: "10.140.140.134"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""  #  Leave empty if not required
          dns: [ ]  #  Leave empty if not required
          port_mapping:
            - name: "IF_BCF_OSP_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_BCF_ESRP"
          fqdn: ""
          ip: "10.140.140.135"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""  #  Leave empty if not required
          dns: [ ]  #  Leave empty if not required
          port_mapping:
            - name: "IF_BCF_ESRP_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_BCF_CHFE"  # Interface between BCF and CHFE
          fqdn: ""
          ip: "10.140.140.136"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_BCF_CHFE_SIP_5060_TCP"
              protocol: "SIP"  # Session Initiation Protocol for call signaling
              port: 5060       # Standard SIP port
              transport_protocol: "TCP"
    # Test System ESRP
    - name: "ESRP"
      mode: "STUB_SERVER"
      function: "ESRP"
      role: ["OTHER"]
      fqdn: ""
      certificate_file: ""
      certificate_key: ""
      api: # All values listed to be added manually
        conference_id: "test_conference_id"
      interfaces:
        - name: "IF_ESRP_BCF"
          fqdn: ""
          ip: "10.140.140.137"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""  #  Leave empty if not required
          dns: [ ]  #  Leave empty if not required
          port_mapping:
            - name: "IF_ESRP_BCF_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_ESRP_CHFE"
          fqdn: ""
          ip: "10.140.140.138"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""  #  Leave empty if not required
          dns: [ ]  #  Leave empty if not required
          port_mapping:
            - name: "IF_ESRP_CHFE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_ESRP_TS-CHFE"
          fqdn: ""
          ip: "10.140.140.139"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""  #  Leave empty if not required
          dns: [ ]  #  Leave empty if not required
          port_mapping:
            - name: "IF_ESRP_TS-CHFE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
    # Test System BCF-RECEIVER
    - name: "BCF-RECEIVER"    # Secondary BCF acting as receiver
      mode: "STUB_SERVER"
      function: "BCF"
      role: ["RECEIVER"]        # Receives SIP responses
      fqdn: ""
      certificate_file: ""
      certificate_key: ""
      interfaces:
        - name: "IF_BCF-RECEIVER_CHFE" # Interface between BCF-RECEIVER and CHFE
          fqdn: ""
          ip: "10.140.140.140"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_BCF-RECEIVER_CHFE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
    # IUT
    - name: "CHFE"             # Call Handling Element under test
      mode: "REAL_DEVICE"     # Physical/real device in the testbed
      function: "CHFE"
      role: ["IUT"]             # Implementation Under Test
      fqdn: ""
      certificate_file: ""
      certificate_key: ""
      api:                    # All values listed to be added manually
        sender_tel_number: "" # Tel number of sender acceptable by CHFE and auto-answered
        tel_number: "" # Tel number of CHFE itself
      interfaces:
        - name: "IF_CHFE_OSP"
          fqdn: ""
          ip: "192.168.49.107"
          mask: "255.255.255.0"
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_CHFE_OSP_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_CHFE_BCF"
          fqdn: ""
          ip: "192.168.49.107"
          mask: "255.255.255.0"
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_CHFE_BCF_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_CHFE_BCF-RECEIVER"
          fqdn: ""
          ip: "192.168.49.107"
          mask: "255.255.255.0"
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_CHFE_BCF-RECEIVER_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_CHFE_LIS"
          fqdn: ""
          ip: "192.168.49.107"
          mask: "255.255.255.0"
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_CHFE_LIS_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
            - name: "IF_CHFE_LIS_HTTP_8080_TCP"
              protocol: "HTTP" # For HELD POST requests to LIS
              port: 8080       # Common alternative HTTP port
              transport_protocol: "TCP"
        - name: "IF_CHFE_ESRP"
          fqdn: ""
          ip: "192.168.49.107"
          mask: "255.255.255.0"
          gateway: ""  #  Leave empty if not required
          dns: []  #  Leave empty if not required
          port_mapping:
            - name: "IF_CHFE_ESRP_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_CHFE_BRIDGE"
          fqdn: ""
          ip: "192.168.49.107"
          mask: "255.255.255.0"
          gateway: ""  #  Leave empty if not required
          dns: []  #  Leave empty if not required
          port_mapping:
            - name: "IF_CHFE_BRIDGE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_CHFE_TS-CHFE"
          fqdn: ""
          ip: "192.168.49.107"
          mask: "255.255.255.0"
          gateway: ""  #  Leave empty if not required
          dns: []  #  Leave empty if not required
          port_mapping:
            - name: "IF_CHFE_TS-CHFE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_CHFE_TS-CA"
          fqdn: ""
          ip: "192.168.49.107"
          mask: "255.255.255.0"
          gateway: ""  #  Leave empty if not required
          dns: []  #  Leave empty if not required
          port_mapping:
            - name: "IF_CHFE_TS-CA_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_CHFE_BRIDGE"
          fqdn: ""
          ip: "192.168.49.107"
          mask: "255.255.255.0"
          gateway: ""  #  Leave empty if not required
          dns: []  #  Leave empty if not required
          port_mapping:
            - name: "IF_CHFE_BRIDGE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
    # Test System LIS
    - name: "LIS"             # Location Information Server (can receive HELD or SIP SUBSCRIBE)
      mode: "STUB_SERVER"
      function: "LIS"
      role: ["RECEIVER"]
      fqdn: ""
      certificate_file: ""
      certificate_key: ""
      interfaces:
        - name: "IF_LIS_CHFE"
          fqdn: ""
          ip: "10.140.140.140"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_LIS_CHFE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
            - name: "IF_LIS_CHFE_HTTP_8080_TCP"
              protocol: "HTTP"
              port: 8080
              transport_protocol: "TCP"
    # Test System BRIDGE
    - name: "BRIDGE"             # Location Information Server (can receive HELD or SIP SUBSCRIBE)
      mode: "STUB_SERVER"
      function: "BRIDGE"
      role: ["RECEIVER"]
      fqdn: ""
      certificate_file: ""
      certificate_key: ""
      api:
        conference_id: ""
      interfaces:
        - name: "IF_BRIDGE_OSP"
          fqdn: ""
          ip: "10.140.140.141"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_BRIDGE_OSP_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_BRIDGE_CHFE"
          fqdn: ""
          ip: "10.140.140.142"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_BRIDGE_CHFE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
        - name: "IF_BRIDGE_TS-CHFE"
          fqdn: ""
          ip: "10.140.140.143"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_BRIDGE_TS-CHFE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
    # Test System Conference App
    - name: "TS-CA"             # Location Information Server (can receive HELD or SIP SUBSCRIBE)
      mode: "STUB_SERVER"
      function: "CHFE"
      role: ["RECEIVER"]
      fqdn: ""
      certificate_file: ""
      certificate_key: ""
      api:
        conference_id: ""
      interfaces:
        - name: "IF_TS-CA_CHFE"
          fqdn: ""
          ip: "10.140.140.144"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_TS-CA_CHFE_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"
    # Test System CHFE
    - name: "TS-CHFE"             # Location Information Server (can receive HELD or SIP SUBSCRIBE)
      mode: "STUB_SERVER"
      function: "CHFE"
      role: ["RECEIVER"]
      fqdn: ""
      certificate_file: ""
      certificate_key: ""
      api:
        tel_number: ""
      interfaces:
        - name: "IF_TS-CHFE_ESRP"
          fqdn: ""
          ip: "10.140.140.145"            # Interface IP address
          mask: "255.255.255.128"          # Network mask
          gateway: ""         # Leave empty if not required
          dns: [ ]            # Leave empty if not required
          port_mapping:
            - name: "IF_TS-CHFE_ESRP_SIP_5060_TCP"
              protocol: "SIP"
              port: 5060
              transport_protocol: "TCP"