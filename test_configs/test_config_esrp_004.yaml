test_config:
  conformance:
    tests:
      - name: "ESRP_004"
        variations:
          - name: "SIP_OPTIONS.xml"
            description: "Verify processing of SIP OPTIONS"
            interfaces:
              - name: "IF_BCF_ESRP"
                port_names: []
              - name: "IF_CHFE_ESRP"
                port_names: []
            params:
              messages:
                - action: "receive"
                  if_name: "IF_CHFE_ESRP"
                  if_port_name: ""  # Optional
                  prep_steps: []
                  type: "SIP"
                  method: "INVITE"
                  response_code: ""
                  http_url: ""  # part after f.e. http://IP:PORT
                  body: ""
                  sipp_scenario: {
                    scenario_file_path:
                      "file.test_suite/test_files/SIPp_scenarios/SIP_RECEIVE/SIP_INVITE_RECEIVE_CHFE.xml",
                    kwargs: {},
                    save_response_as: "ESRP_004_RECEIVE_CHFE_response.log",
                    save_log_as: "ESRP_004_RECEIVE_CHFE.log"
                  }
                  run_in_background: "True"
                - action: "send"
                  if_name: "IF_BCF_ESRP"
                  if_port_name: ""  # Optional
                  prep_steps: []
                  type: "SIP"
                  method: "OPTIONS"
                  response_code: "200"
                  http_url: ""  # part after f.e. http://IP:PORT
                  body: ""
                  sipp_scenario: {
                    scenario_file_path: "file.test_suite/test_files/SIPp_scenarios/SIP_OPTIONS/SIP_OPTIONS.xml",
                    kwargs: {},
                    save_response_as: "ESRP_004_var1_SEND_response.log",
                    save_log_as: "ESRP_004_var1_SEND.log"
                  }
                  run_in_background: "False"
            filtering_options: # optional
                - message_type: "stimulus" # stimulus/output/other
                  src_interface: "IF_BCF_ESRP"
                  dst_interface: "IF_ESRP_BCF"
                  sip_method: "OPTIONS" #  leave empty if not needed
                  http_request_method: "" #  leave empty if not needed
                  response_status_code: "200" #  leave empty if not needed
                  header_contains: ""
                  body_contains: ""
          - name: "SIP_INVITE_with_CANCEL.xml"
            description: "Verify processing of SIP CANCEL"
            interfaces:
              - name: "IF_BCF_ESRP"
                port_names: []
              - name: "IF_CHFE_ESRP"
                port_names: []
            params:
              messages:
                - action: "receive"
                  if_name: "IF_CHFE_ESRP"
                  if_port_name: ""  # Optional
                  prep_steps: []
                  type: "SIP"
                  method: "INVITE"
                  response_code: ""
                  http_url: ""  # part after f.e. http://IP:PORT
                  body: ""
                  sipp_scenario: {
                    scenario_file_path:
                      "file.test_suite/test_files/SIPp_scenarios/SIP_RECEIVE/SIP_INVITE_RECEIVE_CHFE.xml",
                    kwargs: {},
                    save_response_as: "ESRP_004_RECEIVE_CHFE_response.log",
                    save_log_as: "ESRP_004_RECEIVE_CHFE.log"
                  }
                  run_in_background: "True"
                - action: "send"
                  if_name: "IF_BCF_ESRP"
                  if_port_name: ""  # Optional
                  prep_steps: []
                  type: "SIP"
                  method: "INVITE"
                  response_code: "200"
                  http_url: ""  # part after f.e. http://IP:PORT
                  body: ""
                  sipp_scenario: {
                    scenario_file_path:
                      "file.test_suite/test_files/SIPp_scenarios/SIP_INVITE/SIP_INVITE_with_CANCEL.xml",
                    kwargs: {},
                    save_response_as: "ESRP_004_var2_SEND_response.log",
                    save_log_as: "ESRP_004_var2_SEND.log"
                  }
                  run_in_background: "False"
            filtering_options: # optional
                - message_type: "other" # stimulus/output/other
                  src_interface: "IF_BCF_ESRP"
                  dst_interface: "IF_ESRP_BCF"
                  sip_method: "INVITE" #  leave empty if not needed
                  http_request_method: "" #  leave empty if not needed
                  response_status_code: "" #  leave empty if not needed
                  header_contains: ""
                  body_contains: ""
                - message_type: "stimulus" # stimulus/output/other
                  src_interface: "IF_BCF_ESRP"
                  dst_interface: "IF_ESRP_BCF"
                  sip_method: "CANCEL" #  leave empty if not needed
                  http_request_method: "" #  leave empty if not needed
                  response_status_code: "200" #  leave empty if not needed
                  header_contains: ""
                  body_contains: ""
        requirements:
          - name: "RQ_ESRP_033"
            variations: [ "SIP_INVITE_with_CANCEL.xml" ]
          - name: "RQ_ESRP_034"
            variations: [ "SIP_OPTIONS.xml" ]
